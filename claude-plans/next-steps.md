1. Try to fix album art
2. The playlist selector dropdown is not scrollable. It should have a LegendList to make it scroll.
3. The playlist selector down should show albums and playlists in the results in addition to songs.
4. There should be a button on the right of the PlaylistSelector to save the current queue as a playlist. It should open a save dialog to save an .m3u file.
5. The queue area should accept files dropped from Finder.